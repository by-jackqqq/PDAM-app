export const metadata = {
  title: 'Login | PDAM',
  description: 'Login PDAM SMK Telkom Malang',
}

const RootLayout = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div >
}

export default RootLayout
